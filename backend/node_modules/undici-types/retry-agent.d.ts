import Dispatcher from 'undici-types/dispatcher/dispatcher/dispatcher'
impoundici-types/retry-handlerci-types/retry-handlerci-types/retry-handler'

export default RetryAgent

declare class RetryAgent extends Dispatcher {
  constructor(dispatcher: Dispatcher, options?: RetryHandler.RetryOptions)
}
